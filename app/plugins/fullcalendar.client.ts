/**
 * Force FullCalendar core to load and inject styles on app init.
 * Ensures calendar is visible even if the component mounts late.
 */
import '@fullcalendar/core'

export default defineNuxtPlugin(() => {})
